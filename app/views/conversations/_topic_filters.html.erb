<%= link_to 'All', conversations_path(page: @page, topic_id: 'all'), remote: true, method: :get, class: " p-10  #{session[:topic_ids].count == @all_topics.count ? "btn_1" : "btn_2"}" %>
<% @topic_filters.each do |topic| %>
	<%= link_to topic.name, conversations_path(page: @page, topic_id: topic.id), remote: true, method: :get, class: "btn #{session[:topic_ids].include?(topic.id) && session[:topic_ids].count != @all_topics.count ? "btn-dark" : "btn-light"}" %>
<% end %>