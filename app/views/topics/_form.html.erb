<div id='flash'></div>
<%= form_for @topic, html: {remote: true} do |f| %>
    <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, autofocus: true, class: 'form-control', id: "topic_title" %>
    </div>
    <div class='form-group'>
        <%= f.label :description %>
        <%= f.text_field :description, autofocus: true, class: 'form-control', id: "topic_description" %>
    </div>
    <div class="form-group">
        <%= f.collection_select(:theme_id, Theme.all, :id, :to_s, {prompt: 'Select Theme'}, class: 'form-control')%>
    </div>

    <span><%= f.submit "Add!", class: 'btn btn-primary', remote: true %></span>
    <button type='button' class='btn' data-dismiss='modal' aria-hidden='true'>Cancel</button>
<% end %>
